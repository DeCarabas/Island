namespace Volcano.Model
{
    // From the Exult code.
    //
    public enum Palettes
    {
        Day = 0,
        Dusk = 1,
        Dawn = 1, // Think this is it.
        Night = 2,
        Invisible = 3,
        Overcast = 4,
        Fog = 5,
        // 6 looks a little brighter than #2.
        // 7 is somewhat warmer.  Torch?
        Red = 8,  // Used when hit in combat.
        // 9 has lots of black.
        Lightning = 10,
        SingleLight = 11,
        ManyLights = 12,
    }
}
